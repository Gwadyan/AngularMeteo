export class City {
    name: string;
    country: string;
    id: string;
  
    constructor(name: string, country: string, id: string) {
      this.name = name;
      this.country = country;
      this.id = id;
    }
}